# 智筛 AI — 设计文档
**核心痛点：AI筛选结果可信度不足，导致HR不敢用、候选人不服气、合规风险难管控**

---

## 一、问题诊断

背景：2026届秋招收到12000份简历，计划招聘120人，目标到面录取比≤8:1（即进入面试≤960人）。现有AI工具匹配准确率91%，但落地存在结构性障碍：

**四角色诉求冲突是核心矛盾：**

| 角色 | 核心诉求 | 与其他角色的张力 |
|------|----------|----------|
| 业务需求方 | 优先985/QS前100，技能匹配度≥80% | 与候选人代表的反歧视诉求直接冲突 |
| 候选人代表 | 不得按学校筛选，规则须全公开 | 与业务方的"院校优先"偏好对立 |
| 校招运营岗 | AI覆盖≥80%初筛，人工复核≤20%，2周内完成 | 要求系统稳定可信，不能频繁人工纠偏 |
| 雇主品牌岗 | 结果全溯源，零歧视风险，不影响学生体验 | 要求透明与申诉通道，增加运营成本 |

**根本问题：** 业务方想用院校做能力代理指标（因为方便），但这既违反反歧视原则，也损害雇主品牌，且准确率低于直接测量能力。真正的解法不是"要不要用院校"，而是**建立一套让所有角色都信任的、基于能力维度的评估机制**——用更准确的指标替换掉有歧视风险的代理指标。

---

## 二、方案设计

**价值主张：可信筛选（Trustworthy Screening）**——不只让HR筛得快，而是让HR敢用、候选人服气、合规无忧。

**三模块架构：**

**① Rule Builder（规则构建）：** HR输入JD，AI从「任职要求」中逐条提取能力维度（1:1对应，不合并不增加），调整权重后一键锁定。锁定后生成**规则指纹（Hash）**，同步至：公示页（任何人可查）、投递确认邮件（候选人留存快照）、系统不可变日志。公司若背后修改规则，指纹变化即可被察觉。**评分维度包含技能匹配度，但在技术层面不含院校信息**，用能力替代学历代理。

**② Screening Dashboard（筛选工作台）：** AI按锁定规则逐份评分，每条结论追溯到具体维度，HR可覆盖但须留痕记录（覆盖原因强制填写）。实现AI覆盖≥80%、人工复核≤20%。

**③ Candidate View（候选人视图）：** 展示维度通过/未通过（不显示分数），提供申诉入口，附规则指纹供自助验证。候选人可用相同JD独立提取维度、调整权重至一致后生成指纹，与邮件比对——自证规则未被篡改。

---

## 三、AI工具选择理由

**模型：** Claude 3.5 Haiku（Anthropic，通过 OpenRouter 调用）

- **指令遵从性强：** 可精确执行结构化约束（禁止项、输出格式、字数限制），评分理由稳定可追溯
- **纯JSON输出：** 支持 `response_format: json_object`，前端直接渲染，解析错误率极低
- **长文本理解：** 单次处理完整JD+简历，维度提取与评分在同一上下文内完成
- **隐性偏见可控：** 通过在技术层面移除院校名称（而非仅靠 prompt 提示"别用"），从根本上切断院校信号

---

## 四、关键配置

**反歧视技术实现（核心创新）：**

院校名称在进入LLM前从简历文本中**彻底移除**，而非仅在 prompt 中说"不得使用"——后者等于把答案放在面前说"别看"，无法从技术层面提供保障。候选人代表的投诉权利建立在此技术保障之上。

**维度提取Prompt约束：**
- 只读JD「任职要求」，每条要求对应且仅对应一个维度
- 维度label直接从原文摘取，保证重复提取结果一致（Hash可信基础）
- 禁止合并多条要求、禁止凭空增加维度

**简历评分Prompt约束：**
- 每维度独立评分，禁止晕轮效应（若各维度分数高度趋同，视为评分无效）
- 无直接事实证据的维度必须给低分，名校/大厂背景不得替代可观察证据
- 评分档位：85-100（量化突出）/ 70-84（基本符合）/ 55-69（部分满足）/ 0-54（证据不足）

**锁定机制：** FNV-1a Hash，输入为「维度label+权重」的JSON序列，规则改变一字则指纹随之改变。

---

## 五、迭代优化记录

| 版本 | 问题 | 优化措施 |
|------|------|----------|
| V1 | 只输出总分，HR不知为何高/低，不敢采信 | 增加分维度评分 |
| V2 | 理由模糊（"综合表现良好"），无法对话 | 要求每条理由引用规则维度+简历具体事实 |
| V3 | HR覆盖后无记录，引发合规担忧 | 增加覆盖留痕，覆盖原因强制填写 |
| V4 | 候选人看分数产生"差1分"争议 | 候选人视图只显示维度通过/未通过，不显示分数 |
| V5 | "规则锁定"仍是公司单方声明，候选人无法独立验证 | 生成规则指纹+公示页+候选人自助验证Tab |
| V6 | LLM各维度评分高度趋同（晕轮效应），强候选人各项全高 | Prompt增加独立评分要求，显式禁止晕轮效应 |
| V7 | Prompt中禁止院校影响评分，但院校名仍送给LLM | 技术层面彻底移除：院校名不进入LLM输入文本 |
| V8 | 权重配置区五条滑块堆叠，视觉混乱 | 改为颜色条+数字输入，色块与控件直接对应 |
| V9 | Tab 1/4 内容全部平铺，信息密度过高 | 两个 Tab 均重构为三步向导（步骤条+翻页导航），每步聚焦单一任务 |

---

## 六、效果评估

| 指标 | 目标 | 方案达成方式 |
|------|------|-------------|
| 到面录取比 | ≤8:1（≤960人进面） | AI自动过滤，运营岗实时监控通过率调节阈值 |
| AI覆盖初筛 | ≥80% | 规则锁定后全量自动评分，无需逐份人工 |
| 人工复核率 | ≤20% | 仅「待定」区间需HR介入，约占20% |
| 筛选周期 | 2周内 | 批量评分，单份<3秒，12000份约10小时 |
| 就业歧视投诉 | 零投诉 | 院校技术隔离+规则公开+申诉通道，三重保障 |
| 雇主品牌风险 | 无负面舆情 | 拒绝原因维度可查，结果全溯源 |

**关键Demo证明点：** 陈志远（深圳大学·双非·本科）项目经验93分、技术技能92分，高于三位985候选人——系统在技术层面根本看不到院校，评分完全基于简历中可观察的能力事实。这同时满足了业务方（更准确的能力信号）和候选人代表（零院校歧视）的诉求，是本方案化解四角色冲突的核心论据。

---
*约970字*
