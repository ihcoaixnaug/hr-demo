# 智筛 AI — 设计文档
**核心痛点：AI筛选结果可信度不足，导致HR不敢用、候选人不服气、合规风险难管控**

---

## 一、问题诊断

背景：2026届秋招收到12000份简历，计划招聘120人，目标到面录取比≤8:1（即进入面试≤960人）。现有AI工具匹配准确率91%，但落地存在结构性障碍：

**四角色诉求冲突是核心矛盾：**

| 角色 | 核心诉求 | 与其他角色的张力 |
|------|----------|----------|
| 业务需求方 | 优先985/QS前100，技能匹配度≥80% | 与候选人代表的反歧视诉求直接冲突 |
| 候选人代表 | 不得按学校筛选，规则须全公开 | 与业务方的"院校优先"偏好对立 |
| 校招运营岗 | AI覆盖≥80%初筛，人工复核≤20%，2周内完成 | 要求系统稳定可信，不能频繁人工纠偏 |
| 雇主品牌岗 | 结果全溯源，零歧视风险，不影响学生体验 | 要求透明与申诉通道，增加运营成本 |

**根本问题：** 业务方想用院校做代理指标（因为方便），但这既违反反歧视原则，也损害雇主品牌。真正的解法不是"要不要用院校"，而是**建立一套让所有角色都信任的、基于能力维度的评估机制**。

---

## 二、方案设计

**价值主张：可信筛选（Trustworthy Screening）**——不只让HR筛得快，而是让HR敢用、候选人服气、合规无忧。

**三模块架构：**

**Rule Builder（规则构建）：** HR输入JD，AI提取能力维度（如项目经验、技术技能、跨部门协作、表达能力），调整权重后**一键锁定**。锁定后不可修改，规则即契约。这直接回应业务方诉求——维度可以包含"与岗位技能需求匹配度"，但不包含院校层次，用能力代替学历代理。

锁定时系统自动生成**规则指纹（Hash）**并向三个方向同步：① 挂载招聘官网公示页（任何人可查阅）；② 发送至所有投递候选人邮箱（含指纹和锁定时间）；③ 写入系统不可变日志。候选人手里有一份规则快照——公司若背后修改规则，指纹变化即可被察觉，彻底解决"你怎么证明没偷改"的信任问题。

**Screening Dashboard（筛选工作台）：** AI按锁定规则逐条打分，每条结论必须追溯到具体规则维度，HR可覆盖但需留痕记录。实现AI覆盖≥80%初筛、人工复核≤20%的运营目标。

**Candidate View（候选人视图）：** 展示维度结果（不显示分数），提供限时申诉入口。规则公开+申诉通道，直接回应候选人代表的透明化与反歧视诉求。

---

## 三、AI工具选择理由

选用 **Claude claude-sonnet**（Anthropic），原因如下：

- **指令遵从性强：** 可精确执行"禁止院校/性别/年龄影响评分"等约束，不会因训练数据中的院校偏见产生隐性歧视
- **结构化输出稳定：** 支持纯JSON输出，便于前端直接渲染评分理由，减少解析错误
- **长文本理解：** 单次可处理完整JD+简历内容，维度提取与评分可在同一上下文内完成
- **推理可解释：** 每条评分理由可追溯到规则维度，而非模糊的"综合判断"

---

## 四、关键配置

**维度提取Prompt核心约束：**
```
从以下JD中提取3-5个核心评估维度，每个维度需可从简历文本中客观判断。
禁止提取：学历层次、院校名称、性别、年龄、籍贯。
输出格式：JSON数组，含维度名称和评分说明。
```

**简历评分Prompt核心约束：**
```
按以下锁定规则对简历评分（0-100分）。
硬性禁止：院校排名、学历层次、性别、年龄不得影响分数。
每条理由必须引用规则中的具体维度，不得使用模糊表述。
输出格式：纯JSON，含各维度分数和一句话理由。
```

**锁定机制：** 规则发布后写入不可变记录，后续评分调用只读取该版本，防止规则漂移。锁定时对规则内容生成 FNV-1a Hash 指纹（如 `A3F2C801`），同步至公示页和候选人邮件。候选人可将邮件中的指纹与公示页当前指纹比对，一旦不一致即可证明规则被篡改。

---

## 五、迭代优化记录

| 版本 | 问题 | 优化措施 |
|------|------|----------|
| V1 | 只输出总分，HR不知道为什么高/低，不敢采信 | 增加分维度评分 |
| V2 | 理由模糊（"综合表现良好"），无法对话 | 要求每条理由追溯到规则维度 |
| V3 | HR覆盖建议后无记录，引发合规担忧 | 增加覆盖留痕机制，记录修改原因 |
| V4 | 候选人看到分数后产生"差1分"争议 | 候选人视图只显示维度结论，不显示分数 |
| V5 | "锁定"仍是公司单方声明，候选人无法独立验证规则未被篡改 | 锁定时生成规则指纹，同步公示页+邮件快照，候选人可比对验证 |

---

## 六、效果评估

| 指标 | 目标 | 方案达成方式 |
|------|------|-------------|
| 到面录取比 | ≤8:1（≤960人进面） | AI按规则自动过滤，运营岗可实时监控通过率 |
| AI覆盖初筛比例 | ≥80% | Rule Builder锁定后全量自动评分，无需逐份人工 |
| 人工复核率 | ≤20% | 仅边界分段（待定）需HR介入，约占20% |
| 筛选周期 | 2周内完成 | 批量评分，单份简历<3秒，12000份约10小时 |
| 就业歧视投诉 | 零投诉 | 规则公开+院校禁用+申诉通道，三重保障 |
| 雇主品牌风险 | 无负面舆情 | 候选人视图告知维度结果，拒绝原因透明可查 |

**验证方式：** Demo中陈志远（深圳大学，双非）在能力维度得分高于三位985候选人，直接证明系统按能力而非学历评估——这同时满足了候选人代表的反歧视要求，也给了业务需求方比"院校代理"更准确的能力信号。

---
*约980字*
