# 智筛 AI · 项目交接文档

> 面试时间：2026.05.25（周一）腾讯 AI-HR 方向组面
> 项目名：智筛 AI（可信简历筛选系统）

---

## 文件清单

| 文件 | 说明 |
|------|------|
| `demo.html` | **主交付物**。单文件 HTML，浏览器直接打开即可运行，无需任何服务器或安装 |
| `DESIGN_DOC.md` | 设计文档（~980字），覆盖问题诊断、方案设计、AI工具选型、Prompt配置、迭代记录、效果评估 |
| `SCRIPT.md` | 10分钟演示脚本 v2，含口播词、操作指引、5个高频追问预案 |

---

## 技术栈说明

**纯前端，零后端，零依赖安装。**

- React 18 + Babel（browser-mode，CDN加载，无需 Node.js）
- Tailwind CSS（CDN加载）
- 所有候选人数据、评分逻辑、UI 组件全部内嵌在 `demo.html` 一个文件里
- 打开方式：双击 `demo.html` 或拖入浏览器

---

## 项目核心设计

**价值主张：可信筛选（Trustworthy Screening）**

解决的核心矛盾：业务方想用院校做代理指标 vs. 候选人要求反歧视 + HR 不敢用 AI 结果。

### 三大模块

1. **规则构建（Rule Builder）**
   - 选岗位预设 → AI 加载维度 → 调整权重 → 一键锁定
   - 锁定后生成 FNV-1a Hash 指纹，同步公示页 + 候选人邮件
   - 规则不含院校/学历/性别，维度只来自 JD 能力要求

2. **筛选工作台（Screening Dashboard）**
   - AI 按锁定规则打分，每条理由追溯维度
   - HR 可覆盖 AI 建议，但必须填理由（留痕可审计）
   - 汇总栏显示 AI 自动处理率（产品岗/后端岗均达 80%）
   - 「不推进」候选人可加入跨岗位备选池

3. **候选人视图（Candidate View）**
   - 显示维度结论（符合要求/有待提升），**不显示具体分数**
   - 可查看规则指纹，与邮件比对验证规则未被篡改
   - 提供申诉入口

### 信任机制（核心亮点）
- 规则指纹（Hash）：规则改一个字指纹就变，候选人可自行验证
- 公示页（Blob URL）：任何人可查阅当前规则版本
- HR 覆盖留痕：每次人工干预有记录，可审计
- 候选人视图隐藏分数：避免"差1分"数字争议

---

## Demo 候选人设计（四象限覆盖）

### 产品经理岗（A-E）

| 候选人 | 学校 | 层次 | 结果 | 说明 |
|--------|------|------|------|------|
| 王芳 | 复旦大学 | 985 | 强推进 | 强985·有主导项目 |
| 陈志远 | 深圳大学 | 双非 | 强推进 | **核心对比**：双非凭能力胜出 |
| 张浩然 | 东南大学 | 985 | 待定 | 有工具技能，无主导经历 |
| 李思琪 | 浙江大学 | 985 | 不推进 | **核心对比**：985 无产品经历被拒 |
| 刘晓晨 | 广东工业大学 | 双非 | 不推进 | 弱双非·技能不达标 |

### 后端开发岗（F-J）

| 候选人 | 学校 | 层次 | 结果 | 说明 |
|--------|------|------|------|------|
| 吴佳琪 | 杭州电子科技大学 | 双非 | 强推进 | 开源贡献+mini-Redis |
| 赵明远 | 南京大学 | 985 | 强推进 | 字节实习+高并发架构 |
| 林浩宇 | 华中科技大学 | 985 | 待定 | 基础尚可，工程深度不足 |
| 周晓敏 | 西安交通大学 | 985 | 不推进 | 数模竞赛背景，与后端不匹配 |
| 郑凯文 | 河南工业大学 | 双非 | 不推进 | 前端经验为主，后端弱 |

---

## 演示流程（10分钟）

详见 `SCRIPT.md`，简要节奏：

- 0:00–1:00 开场，抛出"AI筛完了，我敢不敢用"的核心张力
- 1:00–3:30 Page 1 规则构建：选岗位 → 加载维度 → 锁定 → 生成指纹 → 查看公示页
- 3:30–7:30 Page 2 筛选工作台：全选运行 → 对比陈志远（双非强推进）和李思琪（985不推进）→ 展开理由 → 演示HR覆盖 → 备选池
- 7:30–9:00 Page 3 候选人视图：陈志远视图 → 指纹验证 → 李思琪申诉
- 9:00–10:00 总结四角色诉求闭环

---

## 待办 / 可继续优化方向

- [ ] **部署上线**：可用 Netlify Drop（把 demo.html 拖进 app.netlify.com/drop）一键发布，获得公开链接分享给评审
- [ ] 如评审追问"AI偏见"问题：引用 HR 覆盖机制 + 陈志远双非强推进作为实证
- [ ] 如评审追问"规则指纹能防篡改吗"：生产环境可接入蚂蚁链可信时间戳做司法存证
- [ ] 如需补充第三个岗位（如运营岗）：在 `JOB_PRESETS` 对象中新增一个 key，加对应维度和候选人数据即可

---

## 给下一个 AI 的提示

- `demo.html` 是主文件，所有逻辑在 `<script type="text/babel">` 标签内
- 关键 state 都在 `App` 组件：`lockedDims`, `fingerprint`, `selectedJob`, `pool`, `overrides`
- `overrides` 已做状态提升（从 ScreeningDashboard 提升到 App），两个页面共享
- 候选人视图跨岗位维度 bug 已修复：用 `JOB_PRESETS[c.job]?.dims` 而非 `activeDims`
- 备选池按钮条件：`finalResult === "不推进"`（只判断最终结果，不判断原始结果）
- 如需修改候选人数据，找 `const CANDIDATES = [...]` 数组
- 如需新增岗位，找 `const JOB_PRESETS = {...}` 对象
